# Eunoiaverse Platform

# Run and deploy

This contains everything you need to run your app locally.
implementation github.com/eunoiaproject/docs
## Run Locally

**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install`
2. cd Eunoiaverse
3. Run the app:
   `npm run dev`
